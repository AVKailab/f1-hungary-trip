{
  "name": "f1-hungary-translate-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxying Dutch->Hungarian translation through Claude",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
